name: ci/cd

on:
  push:
    branches: [master]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    env:
      GET_LAST_VALIDATED_COMMIT_URL: ${{ secrets.GET_LAST_VALIDATED_COMMIT_URL }}
    steps:
      - uses: actions/checkout@v2
      - run: LAST_VALIDATED_COMMIT_HASH=foo
      - run: echo $LAST_VALIDATED_COMMIT_HASH
      - uses: actions/setup-node@v1
        with:
          node-version: "14.x"
      - run: npm install
      - run: npm run lint
